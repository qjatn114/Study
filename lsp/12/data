Run Queue는 속해있는 프로세스들만 관리
pushf   push flag
pushq  64

#define A 10

A;

#define A 10;

A

함수 호출 규약
function calling convention

런타임라이브러리
처음 프로그램이 install되고 시작될 때 라이브러리를 모두 가져다 쓰면 용량이 크고 속도가 느리다
그래서 런타임 라이브러리로 만들면 해당 함수나 해당하는 동작이 작동할 때만 라이브러리를 참조한다는것
즉 자주 참조되지 않지만 용량이 큰 것일 경우 런타임 라이브러리로 만들어 두면 좋고 
런타임 라이브러리는 런타임 환경의 몇몇 행동들을 유발하기 위해서 컴파일에 의해 사용되는 저수준 루틴들의 집합이다.
이것은 런타임 라이브러리로의 호출을 컴파일된 실행 바이너리에 삽입함으로써 가능해진다. 런타임 환경은 실행 모델. 빌트인 함수들 그리고 프로그래밍 언어의 다른 기본 행동들을 구현한다. 
런타임
런타임은 프로그램이 실행되고 있는 때 존재하는 곳을 말한다 즉 컴퓨터 내에서 프로그램이 기동되면 그것이 바로 그 프로그램의 런타임이다. 일부 프로그래밍 언어에서는 어떠한 재사용 가능한 프로그램들이나 루틴들이 런타임 라이브러리로서 하나의 꾸러미로 만들어진다. 이러한 루틴들은 어떠한 프로그램이 실행될 때 거기에 연결되어 그 프로그램에 의해 사용될 수 있다. 프로그래머들은 때로 컴파일 할 때 프로그램 내에 삽입 된 것과 런타임 때 삽임된 것을 구분하는데 전자를 컴파일 타임이라고 부른다.
