HWaddr : mac주소

이 네트워크 망에서 사용하는 bcast
mask  서브넷마스크 네트워크범위를 정해주는것

본인ip 사설 ip

192.168 = 사설ip
사설ip는 인터넷을 쓸 수 없지만 인터넷을 어떻게 쓰지?
=>공인 ip가 맵핑되어있음

tx가 높으면 서버


inet addr 127.0.0.1       


mac주소와 ip주소 차이점

브로드캐스트 

라우터  IP테이블 관리
스위칭은 mac을 베이스로 라우터는 ip를 베이스로 동작

