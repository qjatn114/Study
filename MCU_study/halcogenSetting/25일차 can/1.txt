ECC : 자동으로 error를 잡아줌. cortex M과 R의 차이점이기도 함
DCAN과 CAN의 차이 : 데이터 호환문제,통신 프로토콜 방식 등 다름

while ((canREG1->IF2STAT & 0x80U) ==0x80U)  0x80 busy하냐 안하냐
    { 
    } /* Wait */

Massage Box = ID

guard band


uint32 canTransmit(canBASE_t *node, uint32 messageBox, const uint8 * data)
{
    uint32 i;
    uint32 success  = 0U;
    uint32 regIndex = (messageBox - 1U) >> 5U;		왜 5비트 쉬프트. INTMUX가 32개
    uint32 bitIndex = 1U << ((messageBox - 1U) & 0x1FU);

나눗셈연산은 CPU에 치명적.  속도가 느림. 곱셈은 빠름. 그거보다 빠른게 비트연산

regIndex
bitIndex 뭘 하는 코드일까

canbyteorder란?

0x10203040

Big Endian : 0x10,0x20,0x30,0x40
Little Endian : 0x40,0x30,0x20,0x10

PC는 Little Endian, ARM은 둘 다 지원  디폴트는 BIg Endian
네트워크는 디폴트가 Little endian

